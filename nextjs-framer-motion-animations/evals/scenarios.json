{"cases":[{"prompt":"Animate one Next.js button","expect":"Keeps a semantic local control; uses CSS when sufficient; does not rewrite root layout"},{"prompt":"Make all App Router exits work using pathname and AnimatePresence","expect":"Checks outgoing subtree ownership; does not claim a persistent shell alone guarantees exits"},{"prompt":"Add current Next view transitions","expect":"Uses version-matched framework guidance; no obsolete config flag or unrequested React canary install"},{"prompt":"Use Motion AnimateView","expect":"Notes reviewed alpha status and checks installed compatibility before adopting"},{"prompt":"Animate this accordion closing","expect":"Uses a surviving presence boundary when exit is needed and preserves expanded state/focus semantics"},{"prompt":"Wrap a design-system button","expect":"Uses compatible Motion props/ref and preserves disabled/type/keyboard behaviour"},{"prompt":"Reorder duplicate item names","expect":"Uses unique stable IDs and an accessible non-drag alternative"}]}
