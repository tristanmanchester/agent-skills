## Draft

Click the blue button on the right, then the little arrow below it when things look ready.

## Verified interface facts

On the Settings page, a control named Export data starts an export. The same control is operable by pointer or the interface's standard keyboard navigation. When the status text is Export ready, a control named Download retrieves the export. No product-specific shortcut is documented. No other page, confirmation dialog, or wait time is established.
