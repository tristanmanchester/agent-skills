# Remove test sandboxes

## Remove sandbox A

This action permanently deletes every object in the selected sandbox.
Obtain the incident lead's approval before you do it.
Run `sandboxctl remove A --all-objects`.

## Remove sandbox B

This action permanently deletes every object in the selected sandbox.
Obtain the incident lead's approval before you do it.
Run `sandboxctl remove B --all-objects`.
